% TODO: goal finder, given the stereo images, return whether we see the goal and where it is x§§

function [visible, target_pose] = goal_finder(map, current_pose)

  visible = false;
  target_pose = 'ciao';

end
